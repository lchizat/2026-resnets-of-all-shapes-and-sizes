# 2026-resnets-of-all-shapes-and-sizes

In the folder `linear_experiments_python` you can find the code for obtaining the experimental results in the linear case (code in Python).

In the `nonlinear_experiments_julia` you can find the code for obtaining the experimental results in the nonlinear case (code in Julia).